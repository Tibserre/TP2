name: CI devops 2022 EPF
on:
  #to begin you want to launch this job in main and develop
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  test-backend:
    name : simple-api
    runs-on: ubuntu-22.04
    steps:
      #checkout your github code using actions/checkout@v2.3.
      - name : Checkout repository
        uses: actions/checkout@v2.3.3

      #do the same with another action (actions/setup-java@v2) that enable to setup jdk 11
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
          java-package: jdk
      #finally build your app with the latest command
      - name: Build and test with Maven
        run: mvn -B test --file simple-api/pom.xml


  build-and-push-docker-image:
    needs: test-backend
    runs-on: ubuntu-latest

    steps: 
      - name: Checkout code
        uses: action/checkout@v2.3.3
      
      - name: Build image and push backend
        uses: docker/build-push-action@v2
        with:
          context: ./simple-api
          tags: ${{secrets.DOCKERHUB_ID}}/tp2/simple-api

      - name: Build image and push DB
        uses: docker/build-push-action@v2
        with: 
          context: ./database
          tags: ${{secrets.DOCKERHUB_ID}}/tp2/database
      - name: Build image and push httpd
        uses: docker/build-push-action@v2
        with: 
          context: ./httpd
          tags: ${{secrets.DOCKERHUB_ID}}/tp2/httpd

